export enum Permission { 
    ADMINISTRATOR,
    DEVELOPER,
    MODERATOR,
    USER
}
// Simple enum to keep track of permissions
// Using this, you can easily restrict usage of certain commands to certain roles or users